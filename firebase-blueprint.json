{
  "entities": {
    "CustomImage": {
      "title": "CustomImage",
      "description": "An uploaded reference specimen image for a dragon fruit disease category",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The composite key of format Group_Disease"
        },
        "base64": {
          "type": "string",
          "description": "Compressed base64 JPEG format image data"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Raw ISO timestamp of when compiling this asset"
        }
      },
      "required": ["id", "base64", "updatedAt"]
    }
  },
  "firestore": {
    "/custom_images/{imageId}": {
      "schema": "CustomImage",
      "description": "Collection of community uploaded diagnostic specimen images"
    }
  }
}
